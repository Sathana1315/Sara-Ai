{
  "name": "sara-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "SARA — The Personal AI Assistant. Adaptive Personalized Recommendation Engine",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "demo:reset": "node scripts/demo-reset.js"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}
